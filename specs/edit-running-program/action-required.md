# Action Required: Edit Running Program

No manual steps required for this feature.

All tasks can be implemented automatically.

---

## Notes

This feature uses existing Airtable tables and fields:
- **Programmaplanning table** - Already exists with all required fields
- **Field mappings** - `PROGRAMMAPLANNING_FIELDS` already defined in `api/_lib/field-mappings.js`

No new Airtable configuration needed.
