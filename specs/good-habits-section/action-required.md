# Action Required: Good Habits Section

No outstanding actions.

All API endpoints read from Postgres. Reference data is synced from Airtable via full-sync worker. Usage writes go through Postgres with outbox sync to Airtable.
